name: test 

on:
  pull_request:
    branches:
      - main
  push:
    branches: ["*"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: .github/workflows/shared/setup.yml
      - name: Test
        run: make test -j
