name: build 

on:
  pull_request:
    branches:
      - main
  push:
    branches: ["*"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: .github/workflows/shared/setup.yml
      - name: Build 
        run: make desktop-client -j
